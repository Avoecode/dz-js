
console.log(`Пользователь может водить машину: ${
    (hasLicence = true),
    (age = 20),
    (isDrunk = false), 
    (hasLicence && age >= 18 && !isDrunk) 
    ? 'может' 
    : 'не может'   
    }`);
